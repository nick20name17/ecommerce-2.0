:{$PORT}

root * /srv
file_server
encode zstd gzip

header {
	# static marketing page — cache hard, bust via deploy
	Cache-Control "public, max-age=3600"
	X-Content-Type-Options "nosniff"
	Referrer-Policy "strict-origin-when-cross-origin"
}
