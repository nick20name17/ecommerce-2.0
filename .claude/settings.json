{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if jq -r '.tool_input.command' | grep -q 'git push'; then cd /Users/alex/Documents/projects/ecommerce-2.0 && bun run build 2>&1 | tail -5; EXIT=$?; if [ $EXIT -ne 0 ]; then echo '{\"decision\":\"block\",\"reason\":\"Build failed — fix TypeScript errors before pushing\"}'; fi; fi",
            "timeout": 120,
            "statusMessage": "Running build check before push..."
          }
        ]
      }
    ]
  }
}
